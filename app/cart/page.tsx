import { CartPageClient } from "@/components/cart-page-client";

export const metadata = {
  title: "Cart | MADHU GARMENTS",
};

export default function CartPage() {
  return (
    <main>
      <CartPageClient />
    </main>
  );
}
